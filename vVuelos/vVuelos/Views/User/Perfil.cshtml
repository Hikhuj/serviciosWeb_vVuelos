@model vVuelos.Models.user

@{
    ViewBag.Title = "Perfil";
}

<h2>Perfil</h2>
<hr />

<div class="container">
    <div class="row">
        <dl class="dl-horizontal">
            <dt>Usuario:</dt>
            <dd>@Html.DisplayFor(model => model.username)</dd>

            <dt>Correo Electrónico:</dt>
            <dd>@Html.DisplayFor(model => model.email)</dd>

            <dt>Nombre:</dt>
            <dd>@Html.DisplayFor(model => model.first_name)</dd>

            <dt>Segundo Nombre:</dt>
            <dd>@Html.DisplayFor(model => model.middle_name)</dd>

            <dt>Apellido:</dt>
            <dd>@Html.DisplayFor(model => model.last_name)</dd>

            <dt>Segundo Apellido:</dt>
            <dd>@Html.DisplayFor(model => model.second_last_name)</dd>

            <dt>País:</dt>
            <dd>@Html.DisplayFor(model => model.country.name1)</dd>
        </dl>
    </div>
    <div class="row">
        <a href="@Url.Action("UpdateInformation", "User")" class="btn btn-default">Actualizar Información</a>
        <a href="@Url.Action("ChangePassword", "User")" class="btn btn-default">Cambiar Contraseña</a>
        <a href="@Url.Action("Index", "Home")" class="btn btn-danger">Volver</a>
    </div>
</div>