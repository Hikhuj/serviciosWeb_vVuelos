@model IEnumerable<vVuelos.Models.flight>

@{
    ViewBag.Title = "Departures";
}

<h2>Salidas</h2>
<hr />

<div class="container">
    <div class="row">
        <input type="text" id="searchBox" onkeyup="searchFlight()" placeholder="Buscar vuelos" class="form-control">
    </div>
    <div class="row">
        <table class="table table-hover bg-dark" id="flightsTable">
            <thead class="thead-dark">
                <tr>
                    <th scope="col" class="h5">Vuelo</th>
                    <th scope="col" class="h5">Aerolínea</th>
                    <th scope="col" class="h5">Procedencia</th>
                    <th scope="col" class="h5">Fecha</th>
                    <th scope="col" class="h5">Hora</th>
                    <th scope="col" class="h5">Estado</th>
                    <th scope="col" class="h5">Puerta</th>
                    <th scope="col" class="h5">Precio</th>
                </tr>
            </thead>
            <@foreach (var item in Model)
            {
                <tbody>
                    <tr>
                        <td class="h6">@Html.DisplayFor(modelItem => item.code)</td>
                        <td class="h6">@Html.DisplayFor(modelItem => item.airline.name)</td>
                        <td class="h6">@Html.DisplayFor(modelItem => item.city.name)</td>
                        <td class="h6">@Html.DisplayFor(modelItem => item.arrival_date.Date)</td>
                        <td class="h6">@Html.DisplayFor(modelItem => item.arrival_time)</td>
                        <td class="h6">@Html.DisplayFor(modelItem => item.status)</td>
                        <td class="h6">@Html.DisplayFor(modelItem => item.airport_gates.consecutive_airport_gate_id)</td>
                        <td class="h6">$</td>
                        <td>
                            @Html.ActionLink("Comprar", "Compra", "Reservations", new { id = item.consecutive_flight_id}, new { @class = "btn btn-primary" })
                            @Html.ActionLink("Reservar", "Create", "Reservations", new { @class = "btn btn-default"})
                        </td>
                    </tr>
                </tbody>
            }
        </table>
    </div>
</div>

<!--
<br />
<input type="text" id="searchBox" onkeyup="searchFlight()" placeholder="Buscar vuelos" class="form-control">
<br />
<br />
<table class="table" id="flightsTable">
    <tr>
        <th>
            VUELO
        </th>
        <th>
            Aerolinea
        </th>
        <th>
            Procedencia
        </th>
        <th>
            FECHA
        </th>
        <th>
            HORA
        </th>
        <th>
            ESTADO
        </th>
        <th>
            PUERTA
        </th>
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.airline.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.city.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.arrival_date.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.arrival_time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.airport_gates.consecutive_airport_gate_id)
            </td>
            <td>
                @Html.ActionLink("Comprar","Compra","Reservations", new {id=item.consecutive_flight_id}, null) | 
                @Html.ActionLink("Reservar","Create","Reservations")
            </td>
        </tr>
    }
</table>
-->

<script src="~/Scripts/js/Flights.js" type="text/javascript"></script>
